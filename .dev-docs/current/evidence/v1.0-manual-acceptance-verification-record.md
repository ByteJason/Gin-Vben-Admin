# 1.0.0-dev 全功能手工验收手册验证记录

- 基线：`main@1a62ab0c0aeaaba44e9da45f631f1c4f7f665795`
- 时间：`2026-08-23T08:27:37+0800`
- 范围：手工验收指南、公开文档入口、补丁与回滚；没有把本文规划的手工用例记为实际通过。

## 基线行为

命令：

```bash
if git cat-file -e HEAD:docs/manual-acceptance/1.0.0-dev-end-to-end.md 2>/dev/null; then
  echo BASELINE_GUIDE=present
else
  echo BASELINE_GUIDE=absent
fi
```

输入：`HEAD=1a62ab0c0aeaaba44e9da45f631f1c4f7f665795`。
原样输出：

```text
BASELINE_GUIDE=absent
```

退出状态：`0`。基线副本：

- `evidence/v1.0-manual-acceptance-baseline-files/README.md`
- `evidence/v1.0-manual-acceptance-baseline-files/docs-README.md`
- `evidence/v1.0-manual-acceptance-baseline-files/guide.absent.txt`

基线 SHA-256：

```text
README.md=6f281c61973268de081472fb2d4f85dfb6bb1fb643b93458f7675d37ad53b8d8
docs/README.md=e8e1b1b4ec92e9dc73bab8fae43c04650e116c6484526245f7f11ceeb9d0f622
```

## 修改后行为

结构/链接/敏感值验证命令：

```bash
node <内联验证器：检查 22 个必需 UAT 用例、3 个本地文档、相对链接、结果 schema、SMTP typed slots>
```

输入：`README.md`、`docs/README.md`、`docs/manual-acceptance/1.0.0-dev-end-to-end.md`。
原样输出与退出状态：

```text
MANUAL_STRUCTURE_OK cases=22 files=3
exit=0
```

修改后 SHA-256：

```text
README.md=aedbaceb82d9f3b6c2b8e38b8c39557969a189a164f207c6ffbb2c5be1f96fce
docs/README.md=570755ab234fce8d7474dc95cf933b7155cba24a8e40510fc5423e9c5cca1d88
guide=3ac681578786ba8b5be31b3c209ab59f9cf530fd33b930bb82a729c4d42fb652
```

构建命令：

```bash
node scripts/build.mjs --mode embedded --ui antd
```

原样输出与退出状态：

```text
BUILD_MODE=embedded
BUILD_UI=antd
BUILD_PLATFORM=darwin
BUILD_ARTIFACT=.runtime/build/embedded
BUILD_ASSETS=server/internal/platform/webassets/dist
BUILD_MANIFEST=artifact-manifest.json
BUILD_OK
exit=0
```

另执行 `embedded/all --check`、`standalone/antd --check`、`api_only/antd --check`，均输出 `BUILD_CHECK_OK`，退出状态均为 `0`。

## 自动化回归

### 根级契约

命令：`node --test tests/contract/*.test.mjs`。
原样汇总：

```text
ℹ tests 164
ℹ suites 0
ℹ pass 164
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
exit=0
```

### 安装和管理端 UI smoke

命令与原样汇总：

```text
node --test install/tests/smoke.test.mjs
tests=2 pass=2 fail=0 exit=0

pnpm --dir admin run test:smoke
tests=4 pass=4 fail=0 exit=0
```

### 服务端单元/集成包

命令：

```bash
env GOCACHE=/private/tmp/gva-manual-go-cache GOTMPDIR=/private/tmp \
  go -C server test ./... -count=1
```

第一次在受限沙箱中，`internal/platform/observability` 的 `httptest` 回环监听得到 `bind: operation not permitted`，总退出状态 `1`；其余已完成包均为 `ok`。按相同命令授予本机回环权限后重跑，原样末尾输出：

```text
ok  	example.com/gin-vben-admin/server/internal/transport/http/staticui	5.553s
ok  	example.com/gin-vben-admin/server/internal/transport/http/tasks	5.422s
ok  	example.com/gin-vben-admin/server/migrations	5.624s
ok  	example.com/gin-vben-admin/server/tests/integration	5.327s
exit=0
```

### 临时拓扑命令形状

命令：

```bash
node scripts/prepare-runtime-compose.mjs
docker compose -f .runtime/compose/dev.yaml config --quiet
docker compose -f .runtime/compose/postgres.yaml config --quiet
docker compose -f .runtime/compose/read-write.yaml config --quiet
docker compose -f .runtime/compose/ha.yaml config --quiet
docker compose -f .runtime/compose/observability.yaml config --quiet
```

原样结果：

```text
MANUAL_COMMAND_SHAPE_OK
exit=0
```

这里验证的是 Compose 解析和形状；未将真实复制、选主、SMTP 投递或 Prometheus/Grafana 闭环记为通过。

## 补丁与回滚

补丁：`evidence/v1.0-manual-acceptance.patch`
补丁 SHA-256：`22e5109cda9f08f79f017c8478aad4f72af790977d8923593d28fdcc7a223f18`
补丁字节数：`37774`

命令：

```bash
git apply --unidiff-zero --check --reverse .dev-docs/current/evidence/v1.0-manual-acceptance.patch
```

原样输出与退出状态：

```text
ROLLBACK_MANUAL_ACCEPTANCE_CHECK_OK
exit=0
```

可执行入口：`.dev-docs/current/scripts/rollback-v1.0-manual-acceptance.sh --check|--apply`。

补丁还在临时目录完成“基线 → 修改 → 反向应用”往返，原样输出：

```text
ROUNDTRIP_MODIFIED_GUIDE=present
ROUNDTRIP_BASELINE_RESTORED=exact
exit=0
```
