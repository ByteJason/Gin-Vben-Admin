global:
  resolve_timeout: 5m

route:
  receiver: local-webhook
  group_by: [alertname, job]
  group_wait: 10s
  group_interval: 5m
  repeat_interval: 4h

receivers:
  - name: local-webhook
    webhook_configs:
      - url: http://webhook:8080/hooks/alerts
        send_resolved: true
